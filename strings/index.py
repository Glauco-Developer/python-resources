"""
This script demonstrates the use of the `index()` method in Python to find the first occurrence of a specific substring within a string.

The `index()` method:
- Returns the position of the first occurrence of the specified substring.
- Raises a `ValueError` if the substring is not found.
"""
text = "Python is a high-level, general-purpose programming language"
result = text.index("e")
print(result)
