"""
This script demonstrates the use of the `format()` method in Python to create a formatted string.

Key Concepts:
- The `format()` method allows you to embed variables or values into a string at designated placeholders (`{}`).
- It is a flexible way to construct dynamic strings.
"""
version = 3
formatted_string = "Hello Python{}".format(version)
print(formatted_string) 
